
#include<stdio.h>
int main(void)
{
    int n,i,v,a[10],k;
    printf("enter the limit");
    scanf("%d",&n);
    printf("enter the position");
    scanf("%d",&v);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    printf("%d",a[k-1]);
    return 0;
}
