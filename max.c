#include<stdio.h>
void main()
{
int array[20],i,max,s;
printf("enter the number of elements");
scanf("%d",&s);
printf("enter %d integers",&s);
for(i=0;i<s;i++)
{
scanf("%d",&array[i]);
}
max=array[0];
for(i<1;i<size;i++)
{
if(a[i]>max)
{
max=array[i];
}
}
printf("maximum element is%d",&i);
}
